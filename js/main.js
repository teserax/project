'use strict';

const answer = [];
answer [0] = prompt ('how are you','');
answer [1] = prompt ('how are you','');
answer [2] = prompt  ('how are you','');

console.log(typeof(answer));

const users = `WERT`; 
const name = `steel`;
alert(`привет,${users},${name}`);